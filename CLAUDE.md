# neurohub — реселлинг токенов/API нейросетей (клон provod.ai)

Отдельный, самостоятельный сервис — **не часть AI-HUB и не привязан к
top-top.online**. Клиент регистрируется сам, пополняет рублёвый баланс,
получает API-ключ и обращается к OpenAI-совместимому
`/v1/chat/completions` поверх GPT/Claude/Gemini и т.п. Модель — как у
provod.ai: баланс+токены, не продажа готовых подписок (это отдельная,
более рискованная линия — см. «Незакрытое» ниже, статус не определён).

## Архитектурная развилка с AI-HUB/gateway

AI-HUB/gateway построен вокруг принципа «клиент не выбирает модель» (шлёт
код операции, привязанной к top-top). Здесь ровно наоборот — клиент сам
указывает `model` в запросе, это и есть публичный контракт. Поэтому:
раздельные проекты, раздельные БД, общий только **паттерн** (не код):
LiteLLM Router, себестоимость по `model_prices` с периодами действия,
append-only леджер. `pricing.py` скопирован из gateway дословно (он
провайдеро-агностичен), остальное написано заново под другую бизнес-модель.

**Важное отличие в биллинге:** в gateway цена операции известна ДО вызова
(фиксированный тариф) — кредиты списываются вперёд, при ошибке — refund.
Здесь цена известна только ПОСЛЕ ответа (зависит от длины completion).
Поэтому: перед вызовом только проверяем `balance_rub > 0` (не списываем),
после — списываем по факту (себестоимость × наценка × курс). Баланс может
на last call уйти в минус — следующий вызов уже блокируется той же
проверкой. При ошибке провайдера деньги не списывались — просто закрыть
событие `failed`, без refund-логики.

## Статус (2026-09-02) — первый рабочий срез

Поднято и проверено сквозным тестом (curl, реальный docker compose +
Postgres, без реальных вызовов провайдеров — через `mock_response` litellm):
- Регистрация/логин клиента (сессионная кука), кабинет (Jinja2, как в
  `internal-spend` — не SPA).
- Выпуск/перевыпуск API-ключа (`Authorization: Bearer nh_...`), показывается
  один раз.
- `/v1/chat/completions` — OpenAI-совместимый вход/выход (pydantic-схема с
  `extra=allow`, все параметры кроме `model`/`messages` прозрачно летят в
  LiteLLM), включая стриминг (`stream: true`, SSE) — биллинг по usage из
  финального чанка (`stream_options.include_usage` включается всегда).
  Проверено сквозным тестом (curl, `mock_response`), в т.ч. что стрим
  корректно списывает баланс и попадает в историю вызовов в кабинете.
- Биллинг по факту: `pricing_config` (id=1: `markup_percent`,
  `usd_rub_rate`) переводит себестоимость в рубли; списание видно в
  `wallet_ledger` и в кабинете (таблица вызовов с токенами и суммой).
- Пополнение баланса — только вручную: клиент подаёт заявку
  (`topup_requests`), админ подтверждает после реального поступления денег
  (`/admin/topups`) → зачисление в `wallet_ledger`. Реальный эквайринг
  (ЮKassa/Робокасса) сюда же подключится позже без смены модели данных.
- Проверены граничные случаи: 402 при нулевом балансе, 401 на неверный
  ключ, 404 на неизвестную модель, 403 не-админу на `/admin/topups`.

## Ещё сделано (2026-09-02, вторая волна)

- **Бизнес-обзор для админа** (`/admin/overview`): касса/обязательство/
  признанная выручка/себестоимость/маржа — та же модель учёта, что в
  основном CLAUDE.md AI-HUB, только для neurohub. Плюс разбивка по моделям
  и последние регистрации.
- **Rate limit по API-ключу**: 30 запросов/60 сек (`app/ratelimit.py`,
  настройки в `Settings.rate_limit_*`), в памяти процесса — этого достаточно
  для одного инстанса; если сервис станет многоинстансным, лимит перестанет
  быть общим и нужно будет вынести в Redis. Проверено: 30 проходят, дальше
  429.
- **Fallback между моделями** при ошибках уровня провайдера (rate limit/
  timeout/5xx) — цепочка задаётся в `config/models.yaml` (`model_info.fallback`),
  реализована явным циклом в `llm.chat_completion_with_fallback` (не
  встроенным `router.fallbacks`), чтобы точно знать и корректно
  забиллить, какая модель реально ответила. Работает для обычных и
  потоковых запросов; для стрима — только если ошибка произошла ДО первого
  чанка (обрыв соединения посреди уже отправленного клиенту потока не
  переигрывается). Проверено прямым вызовом с симулированным
  `litellm.RateLimitError`.

## Ещё сделано (2026-09-02, третья волна)

- **Восстановление пароля**: клиент подаёт заявку (`/forgot-password`, не
  раскрывает, зарегистрирован ли email — во избежание перебора), админ
  генерирует новый пароль на `/admin/password-resets` и передаёт клиенту
  вне системы (тот же принцип, что уже работает с пополнением баланса —
  без email-инфраструктуры самостоятельный сброс небезопасен).
- **Автотесты** (`tests/`, pytest): 26 тестов, три файла —
  `test_pricing.py` (копия из gateway, идентичная логика), `test_billing_math.py`
  (наценка/курс, округление), `test_api_flow.py` (сквозные сценарии через
  `TestClient` + SQLite: регистрация, дубли email, неверный пароль,
  недостаточный баланс, пополнение+оплата+списание, стриминг, неизвестная
  модель, невалидный ключ, доступ не-админа, fallback между моделями,
  rate limit, восстановление пароля целиком). Запуск:
  `cd neurohub; python -m venv .venv; .venv\Scripts\pip install -r requirements-dev.txt aiosqlite; .venv\Scripts\python -m pytest tests -q`
  (aiosqlite не в основном requirements.txt — тесты гоняют на SQLite, прод на Postgres).
  Прогнано и на реальном Postgres в docker — совпадает.

## Визуальный дизайн (2026-09-02)

Приложение перевёрстано под тёмную технологичную тему — не MVP-заглушка на
голом CSS, а осознанный дизайн: почти чёрный фон, два акцентных oklch-цвета
(фиолетовый/бирюзовый), Space Grotesk (заголовки) + Manrope (текст) +
JetBrains Mono (API-ключи, токены, деньги). Источник — дизайн-канвас
(артефакт, ссылка у пользователя), перенесён в `app/templates/` вручную,
1:1 по токенам/классам. Добавлен публичный лендинг (`landing.html`) — теперь
на `/` для неавторизованных показывается маркетинговая страница, а не
редирект на `/login` (осознанное изменение поведения, тесты обновлены).
Чат-интерфейс из дизайна — задел на будущее, реальной страницы под него
пока нет (есть только API `/v1/chat/completions`).
Визуально не проверялось в браузере (в этом окружении нет Playwright/браузера) —
только структурно через curl (все страницы отдают 200, ключевые элементы на
месте). Стоит открыть глазами перед показом клиентам.

## Ещё сделано (2026-09-03) — документ Flawless/Flowless, 12 из 14 пунктов

Заказчик прислал документ с планом сервиса (кошелёк, платёжный агент,
API-шлюз, VPN-туннель, 10 доп. фич + отдельные разделы «как легально
замаскировать VPN» и «как провести rub→USDT через крипто-агента, не
называя это криптообменом»). **Разделы про маскировку VPN и сокрытие
крипто-операций от регулятора не реализовывались осознанно** — это не
техническая, а сокрытие сути от регулятора/провайдера (подробности решения
см. в истории чата с гендиром, не здесь). VPN-туннель и «резервный пул
аккаунтов для обхода бана» — тоже нет (обход ToS/блокировки провайдера по
сути, не только по форме). Из оставшихся 12 пунктов сделано:

- **Платёжный агент (`/shop`, `/admin/orders`)** — каталог подписок
  (`Product`), заказ списывает с баланса СРАЗУ (цена известна заранее, в
  отличие от API-биллинга) и создаёт `SubscriptionOrder(status=paid)`,
  админ оплачивает у провайдера вручную и жмёт «оплачено» (`fulfilled`)
  или «вернуть деньги» (`refunded` → возврат в `wallet_ledger`). Сид трёх
  тестовых позиций — `scripts/seed_products.py`, цены ориентировочные.
- **Библиотека промптов (`/prompts`)** — любой клиент публикует «роль»
  (`Prompt.system_prompt` + цена за вызов). В `/v1/chat/completions`
  передаётся `prompt_id` → системный промпт подставляется первым
  сообщением, цена списывается СВЕРХ обычной токенной стоимости, автору
  уходит 50% роялти (`billing.charge_prompt_fee`). Работает и в стриминге.
- **DLP-инспектор (`app/dlp.py`)** — перед отправкой в провайдера вырезает
  похожее на API-ключи/Bearer-токены/приватные ключи/`password=`/ИНН и
  паспорт (только с явной меткой типа «ИНН:», иначе лавина ложных
  срабатываний на обычные числа). Хранится **только список типов** находок
  (`usage_events.dlp_redactions`), не сам секрет — иначе БД сама стала бы
  хранилищем того, что защищаем. Значок «DLP» в истории вызовов кабинета.
- **Архиватор диалогов (`/archive`, публичный `/verify`)** — хранится
  только SHA-256 хэш + метка + время, не сам текст (проверка подлинности
  без раскрытия содержимого). Дубликат текста не создаёт новую запись.
- **Семейный тариф «Репетитор» (`/children/new`, `/children/{id}`)** —
  `Customer.is_child` + `parent_customer_id`. Детский аккаунт тратит с
  баланса РОДИТЕЛЯ (`billing.resolve_billing_customer_id`,
  `UsageEvent.billing_customer_id` отдельно от `customer_id`-«актёра»),
  автоматический системный промпт-репетитор, блокировка прямых запросов
  «напиши сочинение/реферат/эссе» (регексом, до вызова провайдера — деньги
  не тратятся на заблокированный запрос). Родитель видит историю ребёнка.

**Найденный и исправленный по дороге баг (повторно, тот же класс, что
раньше):** `session.rollback()` внутри `billing.start_call`/
`purchase_subscription` при недостатке средств экспайрил **все** объекты
сессии текущего запроса (включая `customer` из auth-зависимости, `product`
из роута) — следующее обращение к их атрибутам роняло `MissingGreenlet`.
Убрали `rollback()` из этих веток совсем: FOR UPDATE-лок снимется сам при
закрытии сессии в конце запроса, это безопаснее, чем ловить экспайринг
задним числом. **Урок на будущее: в любом новом billing-коде, где после
проверки баланса нужно прервать транзакцию — НЕ звать `session.rollback()`
вручную, если дальше в этом же запросе ещё будут читаться атрибуты уже
загруженных объектов.**

Автотесты на все 5 фич добавлены (`tests/test_features_wave2.py`, 10 тестов) —
магазин (недостаток средств/покупка/выполнение/возврат), промпт+роялти,
DLP (редактирует и не редактирует лишнего), архив+verify (включая защиту от
дублей), семейный тариф целиком (блок/разрешено/списание с родителя/запрет
внуков). Итого 36 тестов, все зелёные. По дороге поймал две мелкие ошибки в
самих тестах (не в приложении): в SQLite-БД для тестов не был засеян каталог
`Product` (добавил фикстуру `_seed_products` в `conftest.py`), и проверка
"текста нет на странице" ловила случайное совпадение с CSS-классом
`button.secondary` (поправил на `>second<` с угловыми скобками).

## Ещё сделано (2026-09-03, четвёртая волна) — AI-секретарь в Telegram

Последний из 12 пунктов документа Flawless/Flowless, ранее числившийся
отложенным (нужно было решить канал — Telegram или SIP-звонок). Выбрали
Telegram, реализовано и **проверено вживую на реальном боте**
`@rossi_AIhub_bot` (токен пользователя в `.env`, не коммитится).

- **`app/chatcore.py`** — вынесена общая логика одного нестримингового
  обращения к модели с биллингом (`run_chat_turn`): резолв
  billing_customer_id (для детских аккаунтов), DLP-редактирование,
  `start_call` → вызов через `llm.chat_completion_with_fallback` →
  `finalize_success`/`finalize_failure`. Переиспользуется и Telegram-ботом,
  и (логика идентична, но пока не отрефакторено на общий вызов)
  `/v1/chat/completions` в `main.py`.
- **`app/telegram_bot.py`** — long polling (`getUpdates`), не webhook: сервис
  пока без публичного HTTPS-домена, а polling — исходящие запросы, что
  работает откуда угодно. `/start КОД` — привязка Telegram-чата к аккаунту
  через одноразовый `TelegramLinkCode` (клиент получает код в кабинете,
  код удаляется сразу после использования). Дальше — обычный текст или
  голосовое (распознаётся через `litellm.atranscription`/Whisper) уходит в
  `chatcore.run_chat_turn`, ответ прилетает тем же сообщением в Telegram.
  Ошибки провайдера и недостаток баланса — понятным текстом клиенту, не
  трейсбеком.
- Модели: `TelegramLinkCode`, `TelegramLink` (`app/models.py`), миграция
  `0005_telegram_secretary.py`. Настройки: `TELEGRAM_BOT_TOKEN` (пусто =
  фича выключена — ни роут `/telegram/link`, ни фоновый polling не
  стартуют), `telegram_default_model` (дефолт `gpt-5-mini`).
  `app/main.py`: `lifespan()` поднимает `telegram_bot.poll_loop()` фоновой
  таской, только если токен задан; карточка в `dashboard.html` показывает
  код привязки или подтверждение подключения.
- **Живая проверка целиком**: привязка аккаунта, реальный вызов на
  OpenAI упал `insufficient_quota` (пустой баланс тестового ключа) →
  fallback на `gemini/gemini-3.5-flash` (см. `config/models.yaml`)
  отработал, `200 OK`, реальный ответ ушёл в Telegram, баланс списался по
  факту (`usage_events.status=success`, `charged_rub` посчитан). Заодно
  подтвердили, что `gemini-3.5-flash` — существующий id (сверено списком
  моделей от Google), в отличие от предостережения в «Не сделано» ниже.
- **Автотесты** (`tests/test_telegram_secretary.py`, 7 тестов):
  `chatcore.run_chat_turn` отдельно (биллит и возвращает ответ / бросает
  `InsufficientBalance`), привязка по `/start КОД` (успех, неизвестный код,
  сообщение до привязки), полный путь текстового сообщения через
  `_handle_update` → `chatcore.run_chat_turn` → биллинг (успех и
  недостаток средств). Внешние вызовы замоканы: `send_message` пишет в
  список вместо HTTP на api.telegram.org, `llm.chat_completion_with_fallback`
  возвращает канонический litellm-ответ вместо реального провайдера — тот
  же приём, что `mock_response` у `/v1/chat/completions`, только на уровне
  функции (`chatcore.run_chat_turn` его не пробрасывает, в проде это не
  нужно). Итого 43 теста, все зелёные.
- **Побочно исправлена нехерметичность тестов**: `client`-фикстура заходит
  в `TestClient(app)` как context manager, это триггерит `lifespan()`,
  который (если `TELEGRAM_BOT_TOKEN` непустой) реально стартует
  `poll_loop()` — а он читал бы токен из `.env` на диске разработчика,
  включая настоящий боевой токен. Это означало реальные HTTP-вызовы к
  api.telegram.org на каждом тесте с `client`, плюс риск конфликтовать по
  `getUpdates` с реально работающим ботом. Исправлено в `conftest.py`:
  `TELEGRAM_BOT_TOKEN` жёстко выставлен в тестовое фиктивное значение (тому
  же принципу, что уже был для `DATABASE_URL`/`SESSION_SECRET`), а
  `telegram_bot.poll_loop` подменён на no-op фикстурой уровня сессии.

## Ещё сделано (2026-09-03, пятая волна) — security-аудит и фикс критической дыры

Гендир спросил напрямую «насколько безопасен и не дырявый» — вместо общих
слов прогнали реальный аудит кода (агентом, с чтением файлов и построчными
ссылками, не по памяти). Нашли и **сразу исправили**:

- **Критично: печать денег через промпт с отрицательной ценой.**
  `POST /prompts` принимал `price_rub` без проверки знака;
  `billing.charge_prompt_fee` вычитает `prompt.price_rub` из баланса
  плательщика безусловно — отрицательная цена значит баланс **растёт** при
  каждом вызове. Любой клиент мог публиковать себе промпт с ценой -100000 и
  вызывать его. Исправлено: проверка `price_rub > 0` в роуте (`main.py`) +
  CHECK-констрейнт в БД на уровне схемы (миграция `0006`) — так что баг не
  вернётся даже если кто-то забудет проверку в новом коде. Тем же способом
  закрыт `amount_rub` в `/topups/new` (ниже риском, т.к. проходит через
  ручное подтверждение админом, но тот же класс бага).
- **Высокий: утечка ключа провайдера через LiteLLM kwargs.** Схема
  `/v1/chat/completions` (`extra="allow"`) прозрачно пробрасывала ВСЕ
  дополнительные поля тела запроса в LiteLLM, включая `api_base`/`api_key`/
  `base_url` — клиент мог подменить эндпоинт вызова своим доменом, и сервер
  отправил бы туда настоящий ключ OpenAI/Gemini/Anthropic из `.env` (SSRF +
  утечка секрета). Исправлено: явный allowlist разрешённых параметров
  (`_ALLOWED_EXTRA_PARAMS` в `main.py`) вместо прозрачного проброса —
  специально белый список, а не чёрный, чтобы новый опасный параметр в
  будущих версиях LiteLLM не прошёл сам по себе.
- **Средний: неограниченный перебор пароля на `/login`.** Rate limit в
  `app/ratelimit.py` был подключён только к платному API-эндпоинту (по
  `customer_id`, т.е. уже ПОСЛЕ входа) — сама форма логина не была защищена
  вообще. Добавлена отдельная, гораздо более строгая функция
  `check_login()` (10 попыток / 5 минут, ключ по IP — customer_id тут ещё
  неизвестен, это как раз то, что подбирают).

Не тронуто в этой волне (осознанно, не блокер до реального прод-деплоя, т.к.
сервис пока не имеет публичного HTTPS-адреса): CSRF-токены на формах
(частично митигировано дефолтным `SameSite=Lax` у Starlette-сессии, но не
явной защитой), `Secure`-флаг на сессионной куке (сейчас корректно `False`,
т.к. локально всё по HTTP — включать вместе с реальным TLS на VPS), запись
`admin_id` при подтверждении заявок (сейчас видно, ЧТО подтверждено, но не
КЕМ из админов). Список — чек-лист перед реальным продом, не текущий баг.

Автотесты на все три фикса — `tests/test_security_fixes.py` (4 теста:
отрицательная цена промпта отклоняется, отрицательная сумма пополнения
отклоняется, `api_base`/`api_key`/`base_url` не долетают до LiteLLM при этом
разрешённые параметры проходят, `/login` реально блокируется после серии
неверных попыток). Побочно поймали баг в самих тестах: новый лимит на
`/login` считает по IP, а в тестах все запросы идут с одного синтетического
IP TestClient — без сброса между тестами лимит исчерпывался admin-логинами
из совершенно других тестов и ронял 8 несвязанных тестов. Исправлено
autouse-фикстурой `_reset_rate_limits` в `conftest.py`. Итого 47 тестов, все
зелёные.

## Ещё сделано (2026-09-03, шестая волна) — закрыт security-чек-лист, подключён git

Оставшиеся три пункта из чек-листа после волны 5:

- **CSRF**: не токен в каждую форму (их ~15, легко забыть в новой), а
  `app/csrf.py` — middleware, проверяющий `Origin`/`Referer` против домена
  запроса на всех небезопасных методах. Пропускает запросы вовсе без
  Origin (curl, серверные вызовы, тесты — не сценарий CSRF) и `/v1/`
  (API на Bearer-ключе, не на куке). Одна точка защиты на всё приложение,
  новую форму покрывает автоматически, ничего не нужно менять в шаблонах.
- **Secure-кука и дефолтный секрет**: новый `Settings.environment`
  (`development`/`production`, `.env`). `SessionMiddleware` теперь получает
  `same_site="lax"` явно (было неявным дефолтом) и `https_only` от
  окружения — включится сам при `ENVIRONMENT=production`. При старте с
  `production` и дефолтным `session_secret="change-me"` приложение теперь
  падает явной ошибкой, а не тихо работает с подделываемыми сессиями.
- **Аудит действий админа**: `decided_by_admin_id`/`completed_by_admin_id`
  на `TopupRequest`/`SubscriptionOrder`/`PasswordResetRequest` (миграция
  `0007`) — теперь видно не только ЧТО подтверждено, но и КАКОЙ админ это
  сделал.

Автотесты — `tests/test_security_fixes.py` пополнен: межсайтовый POST с
чужим `Origin` блокируется (403), тот же запрос с правильным `Origin`
проходит, подтверждение заявки админом реально пишет его id. Итого 50
тестов, все зелёные.

**Git**: репозиторий для `neurohub` инициализирован, сделан первый коммит
(`git init` + `.gitignore` уже был на месте — .env/venv/pycache исключены).
Дальше версии кода откатываются штатно (`git log`/`git revert`/ветки), как
и версии схемы БД (Alembic) — до этой волны версионировался только второй
слой.

## Ещё сделано (2026-09-04, седьмая волна) — доработки до продуктива, блок 1: биллинг

Заказчик прислал отдельный документ `neurohub-доработки.md` (VS Code
спецификация «проблема → задача → критерий готовности», не Flawless/Flowless)
с чек-листом до реального продуктива: биллинг, ключи/доступ, деньги
(эквайринг/фискализация), резервный поставщик, прод-инфраструктура. Пункты
1-3 по документу блокируют запуск; начали по порядку с блока «Биллинг»
(1.1-1.4) — самого рискованного по деньгам. Прод-инфраструктура (VPS/домен)
и эквайринг **не начаты сознательно** — требуют реальной покупки/выбора от
заказчика (VPS-провайдер, юрлицо под приём оплаты), не блокируются кодом.

- **1.1 Резерв под вызов**: раньше проверяли только `balance_rub > 0` и
  списывали по факту ПОСЛЕ ответа — N параллельных запросов проходили каждый
  по отдельности, уводя баланс далеко в минус (баг подтверждён и
  воспроизведён тестом). Теперь `UsageEvent.reserved_rub` — оценка стоимости
  ДО вызова (по `max_tokens` клиента или дефолту, `Settings.default_max_output_tokens_estimate`),
  доступный баланс = `balance_rub − SUM(reserved_rub) WHERE status='pending'`
  для того же плательщика. Резерв снимается САМ — как только статус меняется
  на success/failed, строка перестаёт участвовать в сумме, отдельного шага
  не нужно.
- **1.2 Раздельная цена записи в кэш**: `ModelPrice.price_per_1m_cache_write_tokens`
  отдельно от чтения кэша (`price_per_1m_cached_tokens`) — у Anthropic запись
  в кэш дороже обычного input. Извлечение из `cache_creation_input_tokens`
  в `llm.extract_chat_usage` — **не проверено боевым вызовом на Claude**.
- **1.3 Честная политика по обрывам**: обрыв стрима ДО первого чанка — как и
  раньше, 0₽. Обрыв ПОСЛЕ того, как клиенту уже ушла часть контента — раньше
  тоже было 0₽ (реальный баг: можно было оборвать соединение прямо перед
  финальным usage-чанком и получить бесплатный частичный ответ). Теперь
  считаем полученный текст по грубой оценке (~4 символа/токен,
  `pricing.estimate_tokens_from_text`) и списываем — событие помечается
  `billing_estimated=True`, чтобы сверка (1.5, ещё не сделана) отличала это
  от подтверждённого провайдером usage.
- **1.4 Идемпотентность**: заголовок `Idempotency-Key` (как у Stripe, не
  поле тела). `UNIQUE(billing_customer_id, idempotency_key)` — NULL не
  участвует в уникальности ни в Postgres, ни в SQLite, обычные вызовы без
  ключа не ограничены. Повтор с тем же ключом: `pending` → 409 (ещё
  выполняется), `success`/`failed` → возвращается сохранённый
  `response_snapshot`/`response_status_code` той же попытки, провайдер не
  вызывается повторно, деньги не списываются дважды. **Ограничение**:
  снапшот для замены сохраняется только для НЕстримингового ответа —
  повтор стримингового запроса с тем же ключом получает 409 с понятным
  сообщением, что байт-в-байт воспроизвести стрим нельзя, а не тихо
  ломается.

Автотесты — `tests/test_billing_reserves.py` (7 тестов): резерв блокирует
второй вызов, пока первый не завершён; резерв снимается после финализации;
цена записи в кэш считается отдельно от чтения; обрыв стрима с уже
переданным контентом списывает по оценке; обрыв ДО контента — 0₽; повтор с
тем же Idempotency-Key не списывает дважды и возвращает тот же ответ; разные
ключи списывают независимо. Итого 57 тестов, все зелёные. Дополнительно
прогнан состязательный ревью (4 независимых агента по резервам/
идемпотентности/обрывам стрима/математике цен + верификация каждой находки
тремя скептиками) — результаты появятся отдельно после завершения.

## Состязательный ревью биллинга (2026-09-04) — что реально сломалось и как починено

Сразу после волны 7 прогнали 4 независимых агента-ревьюера + верификацию
каждой находки тремя скептиками (Workflow). 21 находка, 20 подтверждено.
После дедупликации (несколько ревьюеров независимо нашли одно и то же) —
**10 разных реальных проблем**, почти все устранены в этой же сессии:

- **Критично: `except Exception` не ловит реальный обрыв соединения.**
  Starlette доставляет отмену как `asyncio.CancelledError`/`GeneratorExit` —
  оба наследуются от `BaseException`, не от `Exception`. Значит вся честная
  политика 1.3 (списывать по факту при обрыве) работала только на
  ИСКУССТВЕННЫХ ошибках провайдера (что и проверял тест), а не на настоящем
  «клиент закрыл вкладку». **Исправлено**: обвязка `try/finally` вокруг всего
  генератора `_stream_chat_completion` — `finally` срабатывает при ЛЮБОМ
  способе выйти из функции, включая эти два случая, и не делает `yield`
  (запрещено при обработке `GeneratorExit`). Тест — реальный `aclose()` на
  генераторе (то, что физически делает Starlette), не `raise` внутри фейкового
  потока.
- **Высоко: упавший процесс целиком (OOM/kill) не выполняет вообще никакого
  Python-кода** — ни `except`, ни `finally` тут не спасают, резерв виснет
  навсегда. **Исправлено**: `app/reaper.py` — фоновая задача (тот же паттерн,
  что `telegram_bot.poll_loop`), подметает `pending`-события старше
  `stale_pending_reap_after_seconds` (600с, с запасом над таймаутом
  провайдера), закрывает как `failed` без списания (мы не знаем, что
  произошло — это не то же самое, что честная оценка при обрыве соединения).
- **Высоко: отсутствие строки прайса обнуляло резерв до 0₽**, полностью
  отключая всю защиту именно в этом случае (не гипотетика — обычное
  состояние при добавлении новой модели до сидирования цены).
  **Исправлено**: `billing.estimate_reserve_rub` — консервативный дефолт
  (`fallback_reserve_rub_when_unpriced`, 50₽) вместо нуля, плюс
  `reserve_safety_margin` (×1.5) сверху на ВСЕ резервы — покрывает заодно и
  то, что оценка ~4 симв/токен занижает кириллицу (реально ближе к ~2-2.5
  симв/токен на BPE-токенизаторах, а это основной язык клиентов) и премию
  за запись в кэш, которую грубая оценка не учитывает по составу.
- **Высоко: `charge_prompt_fee` вообще не участвовал в резерве** — тот же
  класс гонки, что чинили для токенной стоимости, просто в соседнем
  коде-пути (роялти-маркетплейс). **Исправлено**: цена промпта (`prompt.price_rub`)
  теперь передаётся в резерв через `extra_fixed_rub`.
- **Высоко: гонка по Idempotency-Key** — предварительная проверка
  (TOCTOU) не закрывает случай, когда два конкурентных запроса с одним
  ключом проходят её одновременно; второй получал сырой `IntegrityError` →
  500. **Исправлено**: `except IntegrityError` вокруг `start_call` → чистый
  409 «повторите». **НЕ** пытается прочитать выигравшую строку в том же
  запросе — тот же урок про `rollback()` посреди запроса, что уже был
  задокументирован здесь раньше (экспайрит `customer` из auth-зависимости →
  `MissingGreenlet`; поймано и на этом фиксе тоже, с первого раза не учёл).
- **Высоко: кэш-токены не считались для СТРИМИНГОВЫХ вызовов** — успешный
  путь стрима вручную собирал `UsageAmounts` из двух полей вместо
  `llm.extract_chat_usage`, теряя `cached_tokens`/`cache_write_tokens`
  именно там, где добавили для них отдельные цены. **Исправлено**:
  переиспользуется `llm.extract_chat_usage` и для стрима тоже.
- **Высоко: tool-calling контент не учитывался при обрыве** — `content_so_far`
  собирался только из `delta.content`, аргументы function-call идут через
  `delta.tool_calls[].function.arguments`. **Исправлено**: собираются оба.
- **Средне: повтор с тем же Idempotency-Key, но ДРУГИМ телом запроса**,
  молча возвращал чужой кэшированный ответ. **Исправлено**: хэш
  (`model`/`messages`/`prompt_id`) сверяется при повторе, несовпадение — 409.
- **Средне: идемпотентность была завязана на `billing_customer_id` (семью),
  не на актора** — два ребёнка одного родителя с одинаковым ключом получили
  бы чужой ответ. **Исправлено**: `UNIQUE(customer_id, idempotency_key)`.

Автотесты — `tests/test_billing_reserves.py` пополнен 8 тестами на эти
фиксы, включая честную симуляцию обрыва (`await gen.aclose()` на реальном
генераторе, не искусственный `raise`). Итого 65 тестов, все зелёные.

## Ещё сделано (2026-09-05, восьмая волна) — доработки до продуктива, блок 2: ключи и лимиты (2.1-2.2)

- **2.1 Несколько именованных API-ключей**: `ApiKey` больше не «один активный
  ключ на клиента» (старое MVP-ограничение) — `name`, `last_four` (последние
  4 символа сырого ключа для опознания в списке, сам ключ по хэшу не
  восстановить), можно заводить сколько угодно, каждый отзывается независимо
  (`POST /api-keys/{id}/revoke`, немедленно). Создание — тот же роут
  `/api-key/regenerate` (имя сохранено намеренно, все тесты и интеграции на
  него уже завязаны), но больше не деактивирует старые ключи.
- **2.2 Лимиты расхода на ключ**: `daily_limit_rub`/`monthly_limit_rub`
  (настраивает клиент, `POST /api-keys/{id}/limits`) и
  `admin_daily_limit_rub`/`admin_monthly_limit_rub` (потолок админа
  ПОВЕРХ клиентского, не замена — действует минимум из заданных,
  `billing._effective_limit`) — для реакции на подозрительный ключ, не
  дожидаясь клиента. Проверка — `billing.check_api_key_spend_limits`, по
  `charged_rub` (только реально списанное, не резервы), календарные
  сутки/месяц UTC. Новый `UsageEvent.api_key_id` — NULL для вызовов не через
  API-ключ (Telegram-секретарь).
- **Rate limit переведён на ключ**, не на клиента (`ratelimit.check(api_key.id)`
  вместо `customer.id`) — у клиента с несколькими интеграциями одна не
  забивает лимит другой, ровно как просит документ («на каждый ключ»).
- Админ-страница `/admin/api-keys` — все активные ключи всех клиентов,
  форма установки потолка.

Автотесты — `tests/test_api_key_limits.py` (11 тестов): несколько ключей
валидны независимо, отзыв одного не трогает другой, хэш не восстанавливается
(в разметке только last_four), нельзя отозвать чужой ключ, дневной лимит
клиента блокирует, потолок админа режет даже при более высоком лимите
клиента, rate limit независим между ключами одного клиента. Итого 72 теста,
все зелёные.

## Ещё сделано (2026-09-05) — визуальный ребренд в Flawless + светлая/тёмная тема

Заказчик прислал лого «flawless» (жирный округлый курсив, teal-цвет) и
попросил применить бренд + переключатель темы. **Решение по масштабу:
только видимый бренд** — код, папка проекта, БД/докер-сервисы, CLAUDE.md
остаются «neurohub» (осознанно, чтобы не плодить риск что-то сломать в путях/
переменных окружения без необходимости). Видимый текст — везде «Flawless».

- **Шрифт логотипа**: точный шрифт с картинки не идентифицирован (похоже на
  кастомный леттеринг) — ближайшая доступная замена: Google Fonts **Nunito
  ExtraBold Italic** (`.wordmark` класс в `base.html`), применяется ТОЛЬКО к
  самому логотипу (шапка, лендинг, auth-страницы), не ко всем заголовкам —
  остальная типографика (Space Grotesk/Manrope/JetBrains Mono) не тронута,
  чтобы не спорить с существующим тёмным финтех-эстетикой.
- **Цвет**: использован уже существующий `--accent-2` (teal, oklch 191) —
  он и так близок к цвету логотипа, отдельный токен не заводили.
- **Светлая/тёмная тема**: CSS-токены переведены на `:root` (тёмная,
  дефолт) + `:root[data-theme="light"]` (новая светлая палитра, та же
  цветовая семья, инвертированная светлота). Кнопка-переключатель
  (🌓/☀️/🌙) — `position:fixed` в правом верхнем углу, есть на КАЖДОЙ
  странице (кладётся в `base.html` вне блока `{% if customer %}`, так что
  и лендинг, и логин/регистрация её тоже получают без правок в этих файлах).
  Выбор темы — `localStorage`, применяется инлайн-скриптом в `<head>` ДО
  отрисовки (без «мигания» неправильной темы при загрузке). Бейджи
  (`.badge-ok/warn/bad/role`) переведены с захардкоженных oklch на
  CSS-переменные — иначе в светлой теме были бы нечитаемы.
- Переименование текста: все `{% block title %}`, лого в шапке/лендинге/
  auth-страницах, текст Telegram-бота («Я AI-секретарь…», «пополните в
  личном кабинете…»), заголовок FastAPI (виден в `/docs`), пример домена в
  curl на лендинге (`api.flawless.ru`) — везде neurohub → Flawless.

**Не проверено визуально** — в этом окружении нет браузера/Playwright,
только curl/структурная проверка (нужные классы/атрибуты/строки
присутствуют в отдаваемом HTML, брейсы CSS сбалансированы). Открыть в
реальном браузере и сверить с картинкой шрифта — на совести того, кто
проверяет перед реальным трафиком.

## Ещё сделано (2026-09-06) — доработки до продуктива, блок 3: веб-чат (2.3)

Третья дверь входа в кабинет — рядом с API-ключом и Telegram, для клиентов,
которые никогда не видели API-ключа («особенно B2B-база АО», как и просил
документ). **Тот же путь биллинга**, что у `/v1/chat/completions` — те же
`billing.estimate_reserve_rub`/`start_call`/`finalize_success`/`finalize_failure`,
никакой параллельной логики списания, только своя обвязка вокруг тех же
функций (как и `chatcore.run_chat_turn` для Telegram — в кодовой базе уже
устоявшийся паттерн: один вход = свой роут с переиспользованием общих
примитивов, а не общая для всех функция верхнего уровня).

- `GET /chat`, `GET /chat/{id}` — список диалогов (сайдбар) + история
  сообщений. `POST /chat/send` — отправка: `conversation_id` (пусто —
  новый диалог), `model`, `message`, опционально `file`. Отдаёт JSON
  (`{conversation_id, conversation_title, reply}`), фронт — vanilla JS
  (`fetch`+`FormData`), без стриминга (см. ниже почему).
- Новые таблицы `web_conversations`/`web_messages` — история хранится
  целиком (не только хэш, как `DialogueArchive`), нужна для восстановления
  полного контекста при каждом следующем вызове провайдера.
- Детский режим/DLP — тот же `_prepare_messages`, что и у API-ключа: детский
  блок-лист и редактирование секретов работают одинаково во всех трёх дверях.
- **Загрузка файлов** — картинки (как vision `image_url`, base64) и простой
  текст (`.txt/.md/.csv/.json/.log`, читается и дописывается в текст
  сообщения). **PDF/офисные документы НЕ реализованы** — нужна отдельная
  библиотека (pypdf/python-docx), в задаче не был указан конкретный формат.
  Лимит — 5MB на файл.
- **Осознанно без стриминга** (в отличие от `/v1/chat/completions`) — для
  первого среза выбрана простота и надёжность (одна проверенная транзакция
  биллинга на сообщение) вместо повторного использования SSE-генератора с
  `finally`-обвязкой из main.py; отправка ждёт полного ответа, потом
  показывает его целиком. Можно добавить стриминг позже, если понадобится.
- Rate limit/лимиты расхода (2.2) на веб-чат **не распространяются** — они
  привязаны к API-ключу, а у веб-чата его нет (сессионная кука). Тот же
  пробел уже был у Telegram-секретаря, ничего нового не добавляет.

**Важная находка при тестировании — почищен фоновый мусор, не связанный с
самим чатом напрямую:** `lifespan()` отменял `telegram_task`/`reaper_task`
через `.cancel()` без `await` — задача помечалась отменённой, но её
`SessionLocal()`/aiosqlite-хендл не гарантированно успевал закрыться до
того, как тест переходил к следующему `TestClient`. На полном прогоне (82
теста — под сотню `TestClient` подряд) это иногда (не всегда — гонка)
приводило к зависанию совершенно не связанного теста через несколько
десятков итераций после того как что-то накопилось. Поймано ретестами:
один и тот же прогон то проходил за 21с, то зависал на 120+с на одном и том
же тесте. Исправлено — `await` после `cancel()` с подавлением
`CancelledError`. Два подряд чистых прогона по 82/82 после фикса.

Автотесты — `tests/test_web_chat.py` (10 тестов): создание диалога и
списание, история отправляется провайдеру на повторных сообщениях,
недостаточно средств → 402, неизвестная модель → 404, пустое сообщение без
файла → 400, чужой диалог недоступен, текстовый файл дописывается в
сообщение, картинка уходит как vision-контент, детский аккаунт блокируется
как и в API. Итого 82 теста, все зелёные (дважды подряд).

## Ещё сделано (2026-09-06) — доработки до продуктива, блок 4: сверка и маржа (1.5-1.6)

**Нет реальной интеграции с биллингом провайдера** (депозит не подключён —
см. «Не сделано» ниже), поэтому «сверка с поставщиком» в буквальном смысле
документа («сумма списанного с клиентов против суммы, списанной провайдером
с депозита») не реализуема без внешнего API. Вместо этого — честная, полезная
версия на данных, которые УЖЕ считаются на каждый вызов: `cost_usd` (наша
себестоимость по `model_prices`) сверяется с `litellm_cost` (встроенная
контрольная оценка LiteLLM, которая пишется рядом именно для этого — см.
`app/pricing.py`). Большое расхождение сигналит об устаревшем прайсе в
`model_prices`, а не о реальном перерасходе у провайдера — отчёт explicit
об этом ограничении, не выдаёт его за то, чем он не является.

- **`GET /admin/reconciliation?d=YYYY-ММ-ДД`** (по умолчанию — вчера):
  выручка/себестоимость/маржа за день, расхождение cost_usd↔litellm_cost в
  %, подсветка (⚠) при расхождении выше `cost_discrepancy_alert_threshold_pct`
  (дефолт 10%) — навигация по дням вперёд/назад. Разбивка по моделям ЗА ЭТОТ
  ДЕНЬ с тем же подсвечиванием низкой маржи, что и в `/admin/overview`.
  **Покрытие litellm_cost частичное** — не пишется для успешного
  стримингового пути (`_stream_chat_completion` передаёт `litellm_cost=None`)
  — отчёт явно показывает, по скольким из дневных вызовов есть контрольное
  значение, не выдаёт частичные данные за полные.
- **1.6 — маржа по моделям** добавлена туда, где уже был разбор по моделям
  (`/admin/overview`, всё время) — раньше показывал только объём и выручку,
  теперь + себестоимость + маржа ₽/% с подсветкой ниже
  `margin_alert_threshold_pct` (дефолт 15%) или отрицательной.
- «Оповещение» реализовано как **видимый сигнал в самой админке** (красная
  подсветка строки/бейдж), не как проактивное уведомление (email/Telegram) —
  критерий документа буквально требует «видимый сигнал», push не
  запрашивался явно, а добавлять новый канал уведомлений — отдельный, более
  крупный кусок работы.
- Заодно поправлена реальная несогласованность из волны 7: `cost_rub`/
  `by_model` в `/admin/overview` фильтровали `status == 'success'` — событие,
  честно списанное по оценке при обрыве стрима (1.3, `billing_estimated`,
  `status='failed'`), туда не попадало. Теперь фильтр — `charged_rub IS NOT NULL`.

Автотесты — `tests/test_reconciliation.py` (7 тестов): доступ только
админу, суммы корректно скоуплены на день (событие другого дня не
подмешивается), расхождение выше порога подсвечивается, в пределах порога —
нет, низкая маржа по модели подсвечивается, день без единого litellm_cost
не падает и честно показывает это, `/admin/overview` показывает
себестоимость и маржу по моделям. UsageEvent для тестов собирается
напрямую (не через billing.*) — так проще точно контролировать
cost_usd/litellm_cost/created_at, биллинг сам по себе уже покрыт в
test_billing_reserves.py. Итого 89 тестов, все зелёные.

## Смена курса (2026-09-07): сначала ВНУТРЕННИЙ контур, потом внешний

Заказчик уточнил дорожную карту: **этап 1 — Flawless как внутренний кабинет
для СВОИХ разработчиков** (покупают через него доступ к моделям), **этап 2 —
внешние заказчики и решения на базе ИИ**. Это меняет приоритеты: эквайринг,
54-ФЗ, 152-ФЗ и юрлицо — блокеры ТОЛЬКО второго этапа, для первого не нужны
вообще. Зато вылезает то, чего в документе доработок не было: продукт написан
целиком как публичный самообслуживаемый реселлер и под закрытый контур не
приспособлен.

**Полный аудит (2026-09-07)** — 43 агента, 6 направлений, 107 замечаний,
37 критичных/высоких перепроверены поштучно: 27 подтвердились, 9 уточнены,
1 отброшена. Две находки воспроизведены запуском на живом приложении.
Отчёт: https://claude.ai/code/artifact/f525f2be-31c1-4997-b58d-5ab6b8cecb10

Главные подтверждённые дыры (полный список в отчёте):
- регистрация открыта всем, отключить человека нельзя ничем кроме SQL;
- `SESSION_SECRET` остался плейсхолдером, а проверка сравнивала с ДРУГОЙ
  строкой (`"change-me"` против `change-me-session-secret` в .env) и не
  срабатывала никогда, даже в production;
- резервных копий БД нет вообще;
- модель без строки цены расходуется бесплатно и бесконечно;
- детский аккаунт перекачивает кошелёк родителя себе через роялти за промпт
  (воспроизведено: 1000 ₽ → 899,9973 у родителя, +50 ₽ ребёнку за один вызов);
- лимиты расхода обходятся выпуском второго ключа или переходом в веб-чат;
- образ Docker собирается вместе с боевыми ключами (нет `.dockerignore`).

## Сделано по итогам аудита — пункт 1: закрытие входа

- **`SIGNUP_MODE`** (`invite` по умолчанию | `open` | `closed`). В режиме
  `invite` регистрация только по одноразовому коду: новая таблица
  `invite_codes`, страница `/admin/invites` (создать код с пометкой «для
  кого», видно кто и когда использовал). Код гасится **атомарным UPDATE
  с `WHERE used_by_customer_id IS NULL`** — простая проверка «свободен ли»
  перед вставкой клиента оставляла бы TOCTOU-гонку, на которой этот проект
  уже обжигался с Idempotency-Key. При проигранной гонке транзакция просто
  не коммитится (клиент не создаётся); `rollback()` руками не зовём — см.
  урок про экспайр объектов сессии в billing.py.
- **`/admin/customers`** — список людей с отключением/включением аккаунта.
  Работает немедленно и на сессию, и на API-ключи: обе зависимости
  аутентификации перечитывают `Customer.active` на каждом запросе. Раньше
  поле читалось, но **не выставлялось нигде в коде**. Себя отключить нельзя
  (иначе админ теряет доступ и чинить снова только SQL).
- **Проверка `SESSION_SECRET`** переехала в `config.session_secret_is_weak()`:
  список известных плейсхолдеров + минимум 32 символа. В production — падение
  на старте, в development — предупреждение в лог (молча работать с дырявыми
  куками нельзя ни в одном режиме). В `.env` сгенерирован настоящий ключ на
  64 символа, в `.env.example` — пустое значение и команда для генерации.
  `ENVIRONMENT` и `SIGNUP_MODE` добавлены в `.env`/`.env.example` (в
  docker-compose ничего не нужно — там `env_file: .env`).
- Валидация `SIGNUP_MODE` при старте: опечатка в переменной уронит сервис
  сразу, а не превратит режим в «пускать всех» по умолчанию.

Автотесты — `tests/test_signup_gating.py` (15 тестов): все три режима
регистрации, повторное использование кода, генерация кода админом, отключение
аккаунта убивает и сессию, и ключ, отключённый не может войти заново, обратное
включение, запрет отключить себя, недоступность админских маршрутов не-админу,
стойкость ключа сессий (включая ту самую пару строк, на которой ломалась
старая проверка). Итого 104 теста, все зелёные. `conftest.py` выставляет
`SIGNUP_MODE=open`, чтобы остальные тесты могли свободно регистрировать
клиентов; закрытые режимы включаются точечно подменой настройки.

## Сделано по итогам аудита — пункт 2: админ управляет деньгами напрямую

- **`billing.admin_adjust_balance()`** — движение баланса без встречной заявки
  от клиента. Раньше пополнить можно было ТОЛЬКО подтвердив заявку, которую
  клиент подал сам: выдать десяти разработчикам месячный бюджет = десять
  заявок от них и десять подтверждений. Минус разрешён осознанно — после сбоя
  в биллинге нужно уметь и забрать.
- **`entry_type` различает смысл, а не механику**: `topup` = реальные деньги
  пришли (попадает в «Кассу» на `/admin/overview`), `adjustment` = внутренний
  бюджет или исправление (кассой НЕ является). Для внутреннего контура это
  принципиально: начисление бюджета своим разработчикам не должно
  притворяться выручкой. Есть тест ровно на это.
- **`WalletLedger.created_by_admin_id`** — кто провёл запись руками. NULL =
  запись сделал сам сервис (списание за вызов, роялти, покупка). Основание
  (`note`) при ручной операции **обязательно** — движение денег без
  объяснения через полгода не расшифровать.
- **`/admin/customers/{id}`** — карточка человека: баланс, форма изменения,
  последние 50 записей журнала с типом и основанием. Аналитика потребления
  (по моделям, по периодам) — пункт 3, здесь только деньги.
- **`/admin/pricing`** — наценка и курс правятся из интерфейса. Раньше
  единственным способом был SQL в боевой базе, при том что докстринг модели
  утверждал «правится админом, не кодом». Добавлены CHECK-констрейнты
  (`markup_percent >= 0`, `usd_rub_rate > 0`) — теперь опечатку отобьёт база,
  а не только форма. На странице явно сказано, что задним числом ничего не
  пересчитывается (значения копируются в UsageEvent в момент вызова), и
  показан пример пересчёта при текущих настройках.

**Поймано тестами до боя:** пример пересчёта изначально считался в шаблоне,
где литерал `0.01` — это float, а `markup_percent`/`usd_rub_rate` — Decimal;
их произведение роняет рендер `TypeError`, то есть страница цен падала бы у
админа. Расчёт перенесён в код (`_pricing_page_context`), в шаблон приходит
готовое число. Второй провал был ошибкой самого теста (искал сумму по всему
HTML и находил её в плитке «Обязательство», где она законна) — тест переписан
на проверку самой величины «Кассы», а не текста страницы.

Автотесты — `tests/test_admin_money_controls.py` (14 тестов): начисление без
заявки, запись автора в журнал, отрицательная корректировка, бюджет не
считается кассой а пополнение считается, начисленный бюджет реально
разблокирует вызовы, обязательность основания, отказ на нулевую сумму и
поддельный `entry_type`, недоступность не-админу, карточка показывает баланс и
журнал, смена наценки/курса с записью автора, отказ на отрицательную наценку и
нулевой курс, неизменность уже сделанных списаний при смене настроек.
Итого 118 тестов, все зелёные.

## Сделано по итогам аудита — пункт 3: отчёт по людям

Раньше во всей админке было ровно две группировки, и обе по модели: на вопрос
«кто из разработчиков сжёг бюджет» интерфейс не отвечал вообще.

- **`/admin/customers?month=YYYY-MM`** — список людей с расходом за месяц,
  количеством вызовов и балансом, отсортированный по тратам. Сверху итог по
  всем за месяц (сколько компания потратила), навигация по месяцам.
- **`/admin/customers/{id}?month=`** — карточка человека: расход за период и
  за всё время, разбивка **по моделям** (вызовы, списано, себестоимость) и
  **по дням**, плюс уже бывший там журнал движения денег.
- **`/admin/customers.csv?month=`** — выгрузка для бухгалтерии и распределения
  бюджета. Разделитель `;`, запятая в числах и BOM в начале — иначе русский
  Excel открывает файл одной колонкой и портит кириллицу.
- Расход считается по **кошельку** (`billing_customer_id`), а не по автору
  вызова: за детский аккаунт платит родитель, бюджет расходуется у него.
  Есть тест ровно на это.

**Две ловушки, на которые наступили и которые стоит помнить:**
1. Группировка по дням делается **в Python**, а не в SQL. Приведение
   `timestamptz` к дате в Postgres зависит от таймзоны сессии — сутки могли
   бы съезжать на границах. Данных здесь мало (один человек за один месяц),
   так что цена вопроса нулевая, а корректность гарантирована.
2. **Тесты не должны искать числа по HTML.** Тестовая БД общая на весь
   прогон: проверка «42.0000 не встречается на странице за март» прошла в
   одиночку и упала в полном прогоне, потому что 42 ₽ оказалось балансом
   человека из соседнего файла тестов. Числовые проверки переведены на разбор
   CSV — там строка на человека, без угадывания. Тот же промах был и в
   пункте 2 (искали сумму, находили её в плитке «Обязательство»).

Автотесты — `tests/test_admin_reporting.py` (11 тестов): расход за выбранный
месяц, переключение периода, итог по всем, расход ребёнка ложится на кошелёк
родителя, отказ на кривой месяц, разбивка по моделям, разбивка по дням со
сложением вызовов внутри дня, «за всё время» рядом с периодом, содержимое и
заголовки CSV, недоступность выгрузки и карточки не-админу. Итого 129 тестов,
все зелёные.

## Сделано по итогам аудита — пункт 4: потолок расхода на человека

Лимит жил только на API-ключе и проверялся только в `/v1/chat/completions` —
исчерпал, нажал «создать ключ» и работаешь дальше; либо просто ушёл в веб-чат
или Telegram, где проверки не было вообще (их события пишутся с
`api_key_id = NULL`, так что и по ключу их было не поймать).

- **`Customer.daily_limit_rub` / `monthly_limit_rub`** — потолок на КОШЕЛЁК.
  Считается по всем событиям кошелька независимо от того, какой дверью
  пришёл вызов, поэтому вторым ключом или переходом в чат не обходится.
  Ставит **админ** (`/admin/customers/{id}/limits`): это бюджетный контроль
  компании, в отличие от лимитов на ключе, которые ставит сам клиент.
  Для детского аккаунта потолок родительский — он и платит.
- **`billing.check_spend_limits(session, billing_customer_id, api_key=None)`** —
  единая проверка вместо `check_api_key_spend_limits`. Одна точка вызова на
  дверь: именно разрозненность проверок и породила пробел. Проверяются оба
  уровня — сначала кошелёк, потом ключ (если вызов пришёл по ключу); ключевой
  лимит остаётся более узкой рамкой внутри общего потолка. У
  `SpendLimitExceeded` появилось поле `scope`, чтобы ошибка говорила, какой
  именно потолок сработал.
- Четыре почти одинаковых запроса свёрнуты в `_spent_since()` + `_enforce_limits()`.
- **Ограничитель частоты подключён к веб-чату и Telegram** (раньше только API).
  Ключи ведёрок теперь строковые с префиксом: `key:<id>` против
  `customer:<id>`. На голых целых id ключ №5 и человек №5 делили бы один
  счётчик — тест на это есть.
- Индекс `ix_usage_events_billing_customer_created` под сам расчёт потолка
  (SUM по кошельку за период) — объявлен и в модели, и в миграции, чтобы
  схема не разъехалась.

Автотесты — `tests/test_customer_spend_limits.py` (14 тестов): потолок
блокирует API, веб-чат и Telegram; второй ключ не даёт чистого лимита; расход
из чата закрывает и API тоже; дневной лимит не считает вчерашнее; лимит ключа
продолжает работать внутри общего потолка; без настроенных лимитов ничего не
блокируется; расход ребёнка считается родителю; админ ставит и снимает
потолок; отрицательный отвергается; клиент не может поставить его себе сам;
ведёрки частоты не сталкиваются; веб-чат ограничен по частоте.
Итого 143 теста, все зелёные.

## Сделано по итогам аудита — пункты 5-6: бесплатный расход и эксплуатация

### 5. Защита от бесплатного расхода

- **`billing.price_for_call()`** — вызов модели без действующей строки в
  `model_prices` теперь **отклоняется** (503 `model_not_priced`) во всех трёх
  дверях, ДО обращения к провайдеру. Раньше такой вызов проходил и был
  БЕСПЛАТНЫМ: `cost_usd=None` → `charged_rub=None` → баланс не падал →
  проверка «баланс > 0» проходила бесконечно. Достаточно было добавить алиас
  в `models.yaml` и забыть строку цены (или дать истечь `valid_until`).
  Событие при отказе вообще не создаётся — есть тест.
  Дефолтный резерв `fallback_reserve_rub_when_unpriced` при этом **оставлен**:
  он страхует путь, где цена может не доехать иначе — fallback-цепочка увела
  вызов на другую модель уже после старта.
- **`pricing.clamp_output_tokens()`** — провайдеру ВСЕГДА уходит явный предел
  длины ответа (`max_output_tokens_cap`, 4096). Клиентское значение зажимается
  сверху, а не игнорируется. Раньше резерв считался из 4096 токенов, но никто
  не заставлял провайдера в них уложиться: ответ на 64к токенов списывался
  целиком уже ПОСЛЕ факта, без проверки баланса, и уводил его в минус. Резерв
  и запрос читают один и тот же `extra`, поэтому оценка и факт сходятся по
  построению — есть тест и на это.

### 6. Эксплуатация

- **`.dockerignore`** — раньше его не было, и `COPY . .` запекал в слои образа
  `.env` с настоящими ключами OpenAI/Gemini и живым токеном бота, всю историю
  git и 182 МБ виндового `.venv`. Ключи оставались в слое навсегда, даже после
  замены файла. Проверено после сборки: контекст упал **со 184 МБ до 169 КБ**,
  `.env`, `.git` и `.venv` внутри образа отсутствуют.
- **`restart: unless-stopped`** на обоих сервисах: любое падение, OOM или
  перезагрузка хоста больше не означают простой до ручного вмешательства.
- **`/healthz` реально ходит в базу** (`SELECT 1`) и отдаёт 503 при недоступной
  БД; в compose добавлен healthcheck приложения. Раньше отдавалась статичная
  строка «ok» — внешний монитор рапортовал «жив», пока БД лежала.
- **`scripts/backup_db.sh` / `scripts/restore_db.sh`** — резервных копий не
  было вообще, при том что балансы и журнал списаний существуют только в томе
  `pgdata` и восстановить их неоткуда (пополнения подтверждаются кнопкой, а не
  выпиской банка). Дамп в обычном SQL (разворачивается чем угодно), ротация,
  проверка на подозрительно маленький файл. Скрипт **прогнан вживую**: дамп
  содержит все 18 таблиц. `restore_db.sh` после заливки сам проверяет, что
  баланс каждого клиента сходится с суммой его записей в журнале.
  Расписание — на хосте через cron, пример в шапке скрипта.
- **`app/alerts.py`** — оповещения администраторам через уже работающего
  Telegram-бота (отдельный канал не заводили). Проверки крутятся в цикле
  уборщика, отдельная фоновая задача не нужна. Тревожат два условия: вызовы
  без себестоимости за час (расход у провайдера идёт, списаний нет) и
  массовые ошибки вызовов. Антиспам — 6 часов на тип, в памяти процесса.

Автотесты — `tests/test_free_spend_and_ops.py` (14 тестов): отказ на модель
без цены во всех дверях, отсутствие события при отказе, работоспособность
проценённой модели, истёкшая строка цены = отсутствие цены, потолок длины
всегда уходит провайдеру, клиентское значение зажимается сверху и уважается
снизу, альтернативное имя параметра, совпадение резерва с запросом,
`/healthz` смотрит в базу, тревога на вызовы без себестоимости, тишина когда
всё в порядке, антиспам оповещений. Итого 157 тестов, все зелёные.

## Закрыты две денежные дыры сверх плана (2026-09-08)

Обе могли двигать реальные деньги неправильно даже во внутреннем контуре,
поэтому чинились, а не откладывались на второй этап.

### Перекачка кошелька родителя через роялти за промпт

Старая проверка в `charge_prompt_fee` сравнивала `author.id` с `payer.id` и
пропускала связку «ребёнок-автор, платит родитель»: ребёнок публиковал промпт
за 100 ₽, вызывал его своим ключом — родитель терял 100 ₽, ребёнку приходило
50 ₽, в цикле кошелёк вычерпывался. В аудите воспроизведено вживую.

- Сравниваем теперь **кошельки** (`resolve_billing_customer_id`), а не
  аккаунты. Внутри одного кошелька промпт **бесплатен** — ни платы, ни
  роялти: платить самому себе за свой же промпт смысла нет, а половина
  суммы при этом просто исчезала бы из семьи.
- **Роялти платится только из собранных денег**: если плата увела баланс
  плательщика в минус, плата фиксируется (услуга оказана, долг виден в
  журнале), а выплата автору — нет, с записью в лог. Раздавать половину
  того, чего не получили, нельзя.
- Второй рубеж: детскому аккаунту запрещено публиковать промпты
  (`POST /prompts` → 403). Ограниченный субаккаунт, тратящий чужой кошелёк,
  продавцом на маркетплейсе быть не должен.

### Двойная трата через магазин

`purchase_subscription` смотрела на голый `balance_rub` и не видела сумм,
зарезервированных под ещё выполняющиеся вызовы: во время долгого запроса к
модели можно было купить подписку почти на весь баланс — обе операции
проходили, итог минус.

- Расчёт «сколько реально можно потратить прямо сейчас» вынесен в
  **`billing.available_balance()`** и используется и в `start_call`, и в
  покупке. Дублирование этой логики и было причиной расхождения — теперь
  разъехаться нечему.

Автотесты — `tests/test_money_leaks_closed.py` (10 тестов). Важна пара:
один проверяет, что ребёнок больше ничего не получает, второй — что **чужому**
автору роялти по-прежнему приходит (то есть чинили дыру, а не выключили
механику). Плюс: повторные вызовы не дренируют родителя, свой промпт
бесплатен, ребёнок не может опубликовать промпт, роялти не платится из
непокрытого баланса, покупка уважает резервы, покупка проходит когда резервов
нет, резерв и покупка вместе не превышают баланс. Итого 167 тестов, зелёные.

**Грабли (третий раз за сессию, записать крупно):** тестовая БД одна на весь
прогон. Мои `shop1@/shop2@/shop3@test.local` совпали с адресами из
`test_features_wave2.py` — тесты прошли поодиночке и упали в общем прогоне с
409 на регистрации. Все адреса в новом файле теперь с префиксом `leak_`.
**Правило: в новом тестовом файле — уникальный префикс у всех email, и
никаких проверок чисел поиском по HTML.**

## Не сделано (следующие шаги)

- Реальный платёжный эквайринг вместо ручного подтверждения — теперь
  подтверждено отдельным документом «доработки» как блокер запуска (п.3.1),
  не только моё наблюдение. Выбор шлюза упирается в решение про юрлицо для
  приёма оплаты (см. историю чата).
- **Фискализация (54-ФЗ)** — новый пункт из документа «доработки»:
  пополнение без указанной услуги — аванс, нужен чек на аванс и чек зачёта,
  интеграция с облачной кассой. Не начато.
- **Документы для юрлиц** (счёт/акт/закрывающие) — не начато.
- **Магазин подписок (`/shop`) — документ «доработки» прямо просит не
  трогать до отдельной юридической проверки** (оплата иностранной подписки
  в интересах третьего лица + валютный вопрос) — уже реализованная фича,
  но с юридическим предупреждением, добавленным задним числом.
- Мониторинг остатка депозита у провайдера — не начато (нужна реальная
  интеграция с биллингом провайдера — см. волну про сверку выше, там же
  почему полноценная сверка «наш расход vs списание с депозита» пока
  заменена суррогатом на cost_usd/litellm_cost).
- GigaChat/YandexGPT по прямому договору как третий провайдер (внешняя
  зависимость — контракт).
- Единый дашборд с графиками/аналитикой (сейчас только таблицы).
- Деплой на реальный VPS (сейчас только локально) — рекомендован Timeweb
  Cloud для старта (соответствует 152-ФЗ), Selectel как более
  сертифицированная альтернатива.
- Из документа Flawless/Flowless — оставшиеся фичи, требующие внешних
  решений (см. историю чата): переводчик тендеров (нужен контракт с
  Контур.Фокусом), аналитика WB/Ozon (нужны ключи продавца от клиента),
  биржа данных для fine-tuning (самая сложная, двусторонний маркетплейс),
  NFC-визитка (офлайн-логистика). AI-секретарь — готов, см. волну 4.
- Точные litellm model id в `config/models.yaml` и цены в
  `scripts/seed_prices.py` не проверены боевым вызовом — сверить перед
  реальным трафиком (то же самое предостережение, что в AI-HUB/gateway).
  Исключение: `gemini/gemini-3.5-flash` сверен списком моделей от Google
  2026-09-03 (существует) в рамках проверки AI-секретаря — остальные три
  (`openai/gpt-5-mini`, `anthropic/claude-sonnet-4-6`) всё ещё не проверены.
- Независимый внешний security-аудит/pentest перед реальным трафиком с
  деньгами — то, что сделано в волнах 5-6, это разовый прогон агентом с
  чтением кода, не полноценный pentest.
- `https_only=True` на сессионной куке включится сам при
  `ENVIRONMENT=production` в `.env` — не забыть выставить при деплое на VPS
  с настоящим TLS (см. волну 6), иначе кука не отправится по HTTP локально.

## Структура

- `app/models.py` — Customer (role customer/admin, is_child/parent_customer_id),
  ApiKey, ModelPrice, PricingConfig (одна строка id=1), UsageEvent
  (customer_id-«актёр» + billing_customer_id-«плательщик» отдельно),
  TopupRequest, Product/SubscriptionOrder (платёжный агент), Prompt
  (библиотека промптов), DialogueArchive (только хэш), PasswordResetRequest,
  WalletLedger (append-only, entry_type: topup/usage/subscription/refund/adjustment).
- `app/billing.py` — вся логика списания/пополнения/покупки/роялти.
- `app/dlp.py` — редактирование секретов перед отправкой провайдеру.
- `app/llm.py` — обёртка LiteLLM Router (клиент сам указывает alias/model),
  `chat_completion_with_fallback`.
- `app/chatcore.py` — общая логика одного вызова модели с биллингом
  (`run_chat_turn`), используется Telegram-ботом.
- `app/telegram_bot.py` — AI-секретарь в Telegram (long polling, привязка
  аккаунта по коду, текст/голос через Whisper).
- `app/ratelimit.py` — rate limit в памяти процесса.
- `app/main.py` — веб (сессия, Jinja2) + `/v1/chat/completions`.
- `config/models.yaml` — реестр моделей, доступных клиентам напрямую.
- `scripts/manage_admin.py` — завести/обновить админа.
- `scripts/seed_prices.py` — засеять `model_prices` + `pricing_config`.
- `scripts/seed_products.py` — засеять каталог подписок для `/shop`.

## Команды

```bash
cp .env.example .env
docker compose up --build -d
docker compose exec neurohub python scripts/seed_prices.py
docker compose exec neurohub python scripts/manage_admin.py --email you@x.ru --name "Имя" --password ...
# интерфейс: http://localhost:8020
```

Порты (чтобы не конфликтовать с AI-HUB на этой же машине): Postgres 5434,
приложение 8020.
