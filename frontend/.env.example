# Server-side only. Never put provider keys or session secrets in NEXT_PUBLIC_*.
FLAWLESS_BACKEND_URL=http://127.0.0.1:8001
# Recommended behind a TLS reverse proxy: set the exact public frontend origin.
# Without it, origin validation uses the request Host and URL scheme.
# FLAWLESS_FRONTEND_ORIGIN=https://app.example.com
